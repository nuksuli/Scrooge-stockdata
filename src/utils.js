export const getLongestStreak = ({ stockData }) => {
    var i,
        temp,
        streak,
        startDate,
        endDate,
        length = stockData.length,
        highestStreak = 0
    for (i = 0; i < length; i++) {
    }
}